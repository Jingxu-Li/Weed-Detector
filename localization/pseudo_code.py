delta_t = 0.01

def get_status():
    return x_k, y_k, z_k, roll_k, yaw_k, pitch_k

def turn_gnss_to_distance():
    distance = 0
    return distance

def predict(x_k, y_k, z_k, roll_k, yaw_k, pitch_k):
    state = []
    # update state
    return

def correct(x_gnss, y_gnss, a_imu, omega_imu, state):
    # update state
    correct_state = []
    return correct_state
